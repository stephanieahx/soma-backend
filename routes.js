const userController = require('./controllers/userController');
const apiController = require('./controllers/sentimentController');
const sentimentController = require('./controllers/sentimentController');
const chartController = require('./controllers/sentimentController');

module.exports = app => {

    // USERS
    // Register user account

    // Log into user account

    // Delete user account


    // SENTIMENT
    // Search topic on twitter for current (today's) sentiment score

    // Add topic to tracked 

    // TRENDS
    // Show dashboard of trend charts (daily sentiment scores) of topics saved by user
    
}